# deps
node_modules/

# build output
dist/
*.tsbuildinfo

# runtime artifacts (D-026)
.rolepod-mcp/
.rolepod-uiproof/

# parent-plugin marker + evidence (Extension Protocol v1, dogfooding)
.rolepod/

# test + coverage
coverage/
.vitest-cache/

# editor / OS
.DS_Store
.vscode/
.idea/
*.swp

# logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# env
.env
.env.local
.env.*.local

# internal design specs — kept locally only
brief/

# claude harness local state
.claude/

# codegraph index (local)
.codegraph/

# internal working docs (specs / plans / hand-offs) — local only
docs/rolepod/

# opencode per-project config written by scripts/install-opencode.sh --project (machine-specific path)
.opencode/
